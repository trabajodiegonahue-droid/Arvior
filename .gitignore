# ============================================================
# ARVIOR — PHP + MySQL (Hostinger / cPanel)
# No Node, no Composer, no build step.
# ============================================================

# --- Credenciales y configuración sensible (NUNCA subir) ---
config.php
.env
.env.*
!.env.example

# --- Uploads (contenido del usuario) — mantener carpeta vacía ---
uploads/*
!uploads/.gitkeep

# --- Respaldos ---
/respaldo/
*.bak
*.sql.gz

# --- Logs ---
*.log
logs/

# --- Sistema operativo / editores ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
.idea/
.vscode/
*.sublime-*

# --- Herramientas locales de IA / agentes ---
.claude/

# --- PHP ---
*.cache
